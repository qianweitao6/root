@layout('layouts.main')

@section('content')
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-md-5">
        <div class="card my-5">
          <div class="card-body">
            <h1 class="my-5 display-4">登录</h1>
            <form action="{{ route('auth') }}" method="POST">
              @!component('components.form.input', {
                name: 'username',
                label: '用户名',
                placeholder: 'Enter your username',
                getErrorFor,
                hasErrorFor,
                old
              })
              @!component('components.form.input', {
                name: 'password',
                type: 'password',
                label: '密码',
                placeholder: 'Enter your password',
                getErrorFor,
                hasErrorFor,
                old
              })
              {{ csrfField() }}
              <button class="btn btn-primary btn-block my-4" type="submit">提交</button>
            </form>
            <p class="card-text">
              <small class="text-muted">忘记密码了？ <a href="#">找回密码</a></small>
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
@endsection
